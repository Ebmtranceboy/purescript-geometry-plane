package:
  dependencies:
    - arrays: ">=7.3.0 <8.0.0"
    - maybe: ">=6.0.0 <7.0.0"
    - numbers: ">=9.0.1 <10.0.0"
    - prelude: ">=6.0.1 <7.0.0"
    - sparse-polynomials: ">=3.0.0 <4.0.0"
  name: geometry-plane
  test:
    dependencies:
      - assert
      - effect
    main: Test.Main
  publish:
    version: 1.1.0
    license: MIT
    location:
      githubOwner: Ebmtranceboy
      githubRepo: purescript-geometry-plane
workspace:
  package_set:
    registry: 54.0.0
  extra_packages:
    sparse-polynomials: 3.0.0
